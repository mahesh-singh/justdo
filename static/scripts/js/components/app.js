var React = require('react');

var JustDoApp = React.createClass({
    render: function () {
        return (
                <h1>This is just do app</h1>
            );
    }
});

module.exports = JustDoApp;

